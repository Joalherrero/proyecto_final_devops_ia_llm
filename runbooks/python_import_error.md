# Fallo de importación Python

Comprueba que el paquete está instalado en la imagen activa. Revisa el directorio de trabajo, la estructura de paquetes y `PYTHONPATH`. Reproduce el import dentro del contenedor antes de reconstruirlo.
